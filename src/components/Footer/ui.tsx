import {IonFooter} from "@ionic/react";

export const Footer = () => {
    return <IonFooter
        className={'fixed bottom-0 left-0 right-0 px-[34px] pb-4 pt-3 rounded-t-[15px]'}>

    </IonFooter>
}
