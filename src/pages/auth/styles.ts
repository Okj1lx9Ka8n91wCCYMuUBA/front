import styled from 'styled-components'

export const AuthStrategyButton = styled.button`
	width: calc(min(100vw - 32px, 353px));
	height: 52px;
	border-radius: 20px;
	font-size: 18px;
	font-weight: 500;
	padding: 11px 30px;
	display: flex;
`
